# ── Estrategia de versionado ──────────────────────────────────────────────────
# ==  : Fijado (pinned) - crítico o frágil conocido; actualizar manualmente
# ~=  : Release compatible (p.ej. ~=2.0.36 permite 2.0.x pero no 2.1)
# >=  : Suelo mínimo - API estable, se esperan actualizaciones compatibles hacia atrás

# Web framework
fastapi>=0.119.0,<0.142.0
starlette~=1.6.0
uvicorn[standard]~=0.32.1

# Config & validation
pydantic[email]~=2.11.0
pydantic-settings~=2.7.0

# Logging
structlog==24.4.0

# HTTP client (LLM gateway + general)
httpx==0.28.1          # Pinned: streaming API is critical path
h2>=4.4.1               # Floor: httpx[http2] transitive dep, CVE fix

# Retry + resilience (LLM gateway)
tenacity~=9.0.0

# Database
sqlalchemy==2.0.36     # Pinned: ORM is foundational, minor updates can break async
alembic~=1.14.0
aiomysql~=0.3.0        # Async MySQL driver (producción + desarrollo Docker)
aiosqlite~=0.20.0      # SQLite async para test suite

# Redis
redis==5.2.1           # Pinned: async API changes between minors

# Security
PyJWT~=2.13.0
bcrypt~=4.0.1
python-multipart~=0.0.20
cryptography~=46.0.6

# File handling
aiofiles~=24.1.0
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"

# Parsing - PDF (pymupdf texto plano primario, pymupdf4llm fallback para scans)
# OJO: NO añadir docling - arrastra torch/CUDA/easyocr (~5 GB) y el parser no lo usa.
pymupdf==1.24.11       # Pinned: C extension, sensitive to updates
pymupdf4llm==0.0.17    # Pinned: pre-stable version

# Parsing - DOCX
python-docx~=1.1.2

# Export - XLSX (generación de reportes; no se aceptan hojas de cálculo como
# fuentes de ingesta desde que se eliminó spreadsheet.py)
openpyxl~=3.1.5

# Export - PDF generation
reportlab~=4.2.0

# Chunking
langchain-text-splitters~=0.3.3
langchain-core~=1.6.1

# Embeddings - multilingual-e5-large via fastembed (usa onnxruntime CPU por defecto)
fastembed==0.4.2       # Pinned: pre-1.0, model loading can change

# Vector store
qdrant-client==1.17.1  # Pinned: must match Qdrant server version in docker-compose

# LLM Gateway - httpx directo con adapters por proveedor.

# Corrective RAG orchestration
langgraph~=1.0.10
langgraph-checkpoint~=3.0.0
langgraph-sdk~=0.3.15

# Guardrails - PII detection (Spanish)
presidio-analyzer~=2.2.0
presidio-anonymizer~=2.2.0

# Email (SMTP async)
aiosmtplib~=5.1.2

# Testing
pytest~=9.0.3
pytest-asyncio~=1.4.0
pytest-cov~=6.0.0
fakeredis~=2.26.1
