[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = module
markers =
    slow: carga un modelo real (ONNX) en vez de mockearlo; excluir con -m "not slow" en corridas rápidas
